- name: variables MUST be substituted to all parts of test
  method: "{{ $methodVar }}"
  path: "/test/vars-usage/{{ $someVar }}"
  query: "?param={{ $someVar }}"
  headers:
    Header1: "{{ $someVar }}"
  request: >
    {
      "reqParam": "{{ $someVar }}"
    }
  response:
    200: >
      {
        "data": "{{ $someVar }}"
      }
  variables:
    methodVar: GET
    someVar: "some-value"
