- name: loader MUST fail if cannot substitute requestArgs to request headers
  method: POST
  path: /some/path
  headers:
    X-Header: "{{ .foo }"
  request: "requestbody"
  response:
    200: "somebody"
  cases:
    - requestArgs:
        foo: 'value 1'
        bar: other1
