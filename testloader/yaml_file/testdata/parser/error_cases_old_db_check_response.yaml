- name: loader MUST fail if cannot substitute dbResponseArgs to query response
  method: GET
  path: /some/path
  response:
    200: "aaabbb"
  dbQuery: "SELECT ALL"
  dbResponse:
    - "{{ .bar } item1"
    - "{{ .bar }} item2"
  cases:
    - dbResponseArgs:
        bar: value1
