- name: loader MUST fail if cannot substitute requestArgs to request query
  method: POST
  path: /some/path
  query: "?name={{ .foo }"
  request: "requestbody"
  response:
    200: "somebody"
  cases:
    - requestArgs:
        foo: 'value 1'
        bar: other1
